import fs from 'fs'; import { projects } from './src/data/projects.js'; const baseUrl = 'https://adegoke-adewale.me'; const urls = ['/']; projects.filter(p => p.visible).forEach(p => urls.push('/projects/' + p.slug)); const xml = \<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">\ + urls.map(url => \<url><loc>\\</loc><changefreq>weekly</changefreq></url>\).join('') + '</urlset>'; fs.writeFileSync('public/sitemap.xml', xml);
