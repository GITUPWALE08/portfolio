You are implementing an engineering portfolio for Adegoke Adewale Idris.

The portfolio's primary identity is:

SYSTEMS & AUTOMATION ENGINEER

Core positioning:

"I build real-world products and the systems behind them."

Follow the specification as the source of truth.

Do not invent project achievements, metrics, technologies, employers,
certifications, URLs, or responsibilities.

Do not turn architectural targets into production claims.

Do not over-engineer the application.

Use React + TypeScript + Vite + Tailwind + Framer Motion.

Do not use Next.js.

Do not introduce a backend, CMS, database, authentication system,
or admin panel unless explicitly required by the specification.

All portfolio content must be data-driven.

Projects, experience, skills, education, certifications and services
must be stored separately from UI components.

Adding a snew project should only require editing the project data.

Use reusable components.

Do not create project-specific components such as IoTBridgeCard,
EntercomCard, etc.

Use React Router for project detail pages.

Prioritize performance and accessibility.

Keep animations subtle and professional.

The site should feel like an engineering portfolio, not a generic
developer template.

Do not use fake skill percentages.

Do not use generic marketing language.

Prefer concrete engineering explanations.

Do not expose confidential information from client/private projects.

Clearly distinguish implemented, tested, designed, planned and target
functionality.

Do not claim that a system is production-scale unless the provided
project information explicitly supports that claim.

Do not change the technology stack without a strong technical reason.

Before adding a dependency, determine whether the feature can be
implemented with the existing stack.

Keep the implementation simple enough to maintain manually.

Do not add features merely because they are common in portfolio
templates.

The portfolio should demonstrate engineering judgement through its
own architecture.